/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ForcePipe } from './force.pipe';

describe('ForcePipe', () => {
  it('create an instance', () => {
    const pipe = new ForcePipe();
    expect(pipe).toBeTruthy();
  });
});
